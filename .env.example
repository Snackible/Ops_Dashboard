# Copy to .env.local and fill in after deploying the Apps Script web app
# (see apps-script/Code.gs). Leave unset to keep running on the local mock
# backend (localStorage) — see README for what that means.
VITE_SHEETS_API_URL=
# Only needed if you set an API_TOKEN script property in Apps Script.
VITE_SHEETS_API_TOKEN=
