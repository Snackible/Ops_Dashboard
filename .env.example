# Copy to .env.local and fill in from your Supabase project settings
# (Project Settings → API). Leave unset to keep running on the local
# mock backend (localStorage) — see README for what that means.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
